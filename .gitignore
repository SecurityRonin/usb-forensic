# Rust build output
/target
**/*.rs.bk
Cargo.lock.bak
# Library crate: resolve the graph fresh (do NOT commit the lock for a lib)
Cargo.lock

# MkDocs build output
/site

# gitignored forensic test data (provenance lives in docs/corpus-catalog.md)
/tests/data/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Local evidence / scratch (never commit case data)
/evidence
/scratch
*.hve
*.plaso
